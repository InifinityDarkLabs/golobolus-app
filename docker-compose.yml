version: '3.5'

services:

  golobolus-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: golobolus-app:latest
    volumes:
      - '.:/usr/src/app'
      - '/usr/src/app/node_modules'
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=development
